Описание задачи 1
Написать программу, которая на вход принимает 2 параметра: начало и конец диапазона. Затем эта функция перебирает в себе числа в этом диапазоне и выводит на экран по следующему принципу:
- если число кратно трем, на экран выводит слово "физ",
- если число кратно 5, то выводится слово "баз",
- если кратно и 3 и 5 одновременно - "физбаз",
- в остальных случаях - выводится само число.

Формат входных данных

Через консоль вводится единственная строка, состоящая из двух целых чисел, разделенных запятой и пробелом.
В классе Task1 нужно написать методы task1 с модификатором доступа protected и main.
В task1 реализована вся логика задачи. На вход метод принимает строку с двумя целыми числами, разделенными запятой и пробелом и возвращает значение string.
В main реализован консольный ввод строки и вывод в консоль результата.

Формат выходных данных
В единственной строке выходных данных выводятся значения через запятую и пробел согласно условию задачи.

Примеры

Пример 1
Ввод: 4, 16
Вывод: 4, баз, физ, 7, 8, физ, баз, 11, физ, 13, 14, физбаз, 16

Пример 2
Ввод: -15, -5
Вывод: физбаз, -14, -13, физ, -11, баз, физ, -8, -7, физ, баз

Пример 3
Ввод: 15, 0
Вывод: физбаз, 14, 13, физ, 11, баз, физ, 8, 7, физ, баз, 4, физ, 2, 1, физбаз